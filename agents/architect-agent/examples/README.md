# Examples

Examples are added from real runs, not crafted upfront.

Each example pair covers one output state:
- `run-{NNN}-input.json` — the WorkItem fed to the Architect
- `run-{NNN}-output.json` — the DesignDoc or escalation output produced

Populated after the first acceptable Architect runs.
